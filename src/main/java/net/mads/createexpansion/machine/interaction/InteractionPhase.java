package net.mads.createexpansion.machine.interaction;

/** Defines when a machine or recipe interaction is evaluated. */
public enum InteractionPhase {
    ON_START,
    WHILE_PROCESSING,
    ON_COMPLETE
}
